CORS_ORIGINS=http://localhost:3000
MAX_IMAGE_SIZE_MB=25
# Required for POST /api/analyze/image. Verify checkpoint card labels and license first.
DEEPFAKE_MODEL_ID=
MODEL_VERSION=reviewed-detector-checkpoint
